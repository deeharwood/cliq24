<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN" monitorInterval="30">
    
    <!-- Properties -->
    <Properties>
        <Property name="LOG_PATTERN">%d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %-5level %logger{36} - %msg%n</Property>
        <Property name="APP_LOG_ROOT">logs</Property>
    </Properties>
    
    <!-- Appenders -->
    <Appenders>
        <!-- Console Appender -->
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="${LOG_PATTERN}"/>
        </Console>
        
        <!-- File Appender - All Logs -->
        <RollingFile name="FileAppender" 
                     fileName="${APP_LOG_ROOT}/cliq24.log"
                     filePattern="${APP_LOG_ROOT}/cliq24-%d{yyyy-MM-dd}-%i.log">
            <PatternLayout pattern="${LOG_PATTERN}"/>
            <Policies>
                <TimeBasedTriggeringPolicy interval="1"/>
                <SizeBasedTriggeringPolicy size="10MB"/>
            </Policies>
            <DefaultRolloverStrategy max="10"/>
        </RollingFile>
        
        <!-- File Appender - Error Logs Only -->
        <RollingFile name="ErrorAppender" 
                     fileName="${APP_LOG_ROOT}/cliq24-error.log"
                     filePattern="${APP_LOG_ROOT}/cliq24-error-%d{yyyy-MM-dd}-%i.log">
            <PatternLayout pattern="${LOG_PATTERN}"/>
            <Policies>
                <TimeBasedTriggeringPolicy interval="1"/>
                <SizeBasedTriggeringPolicy size="10MB"/>
            </Policies>
            <DefaultRolloverStrategy max="10"/>
            <Filters>
                <ThresholdFilter level="ERROR" onMatch="ACCEPT" onMismatch="DENY"/>
            </Filters>
        </RollingFile>
        
        <!-- Async Appenders for better performance -->
        <Async name="AsyncConsole">
            <AppenderRef ref="Console"/>
        </Async>
        
        <Async name="AsyncFile">
            <AppenderRef ref="FileAppender"/>
        </Async>
        
        <Async name="AsyncError">
            <AppenderRef ref="ErrorAppender"/>
        </Async>
    </Appenders>
    
    <!-- Loggers -->
    <Loggers>
        <!-- Root Logger -->
        <Root level="INFO">
            <AppenderRef ref="AsyncConsole"/>
        </Root>

        <!-- Cliq24 Application Loggers -->
        <Logger name="com.cliq24.backend" level="DEBUG" additivity="false">
            <AppenderRef ref="AsyncConsole"/>
        </Logger>

        <!-- Spring Framework -->
        <Logger name="org.springframework" level="INFO" additivity="false">
            <AppenderRef ref="AsyncConsole"/>
        </Logger>

        <!-- Spring Security -->
        <Logger name="org.springframework.security" level="DEBUG" additivity="false">
            <AppenderRef ref="AsyncConsole"/>
        </Logger>

        <!-- MongoDB -->
        <Logger name="org.springframework.data.mongodb" level="INFO" additivity="false">
            <AppenderRef ref="AsyncConsole"/>
        </Logger>
        
        <!-- Hibernate Validator -->
        <Logger name="org.hibernate.validator" level="WARN" additivity="false">
            <AppenderRef ref="AsyncConsole"/>
        </Logger>
        
        <!-- Reduce noise from these libraries -->
        <Logger name="org.apache.catalina" level="WARN"/>
        <Logger name="org.apache.tomcat" level="WARN"/>
        <Logger name="org.apache.coyote" level="WARN"/>
    </Loggers>
    
</Configuration>
