package com.cliq24.backend.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class OAuth2Config {
    // OAuth2 configuration is handled via application.properties
    // and Spring Security configuration in SecurityConfig
}
